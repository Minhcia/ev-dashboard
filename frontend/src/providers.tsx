"use client";
import React from "react";

export default function Providers({ children }: { children: React.ReactNode }) {
  // Add AuthProvider, ThemeProvider, QueryClientProvider, etc. here as needed
  return children;
} 