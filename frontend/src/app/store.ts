// Global state management setup (Redux/Zustand/etc.)
// Example with Zustand:
// import { create } from 'zustand';
// export const useStore = create((set) => ({ count: 0, inc: () => set((state) => ({ count: state.count + 1 })) }));

export {}; 